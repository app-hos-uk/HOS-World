# Railway Configuration for Search Microservice

[build]
builder = "DOCKERFILE"
dockerfilePath = "services/search/Dockerfile"

[deploy]
healthcheckPath = "/health/live"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
