/**
 * Sole allowlisted home for the platform default ISO 4217 currency code.
 * Business logic should import this (or resolve via PlatformRegionService) instead
 * of embedding 'USD' / 'GBP' / … string literals.
 */
export const PLATFORM_DEFAULT_CURRENCY = 'USD';
