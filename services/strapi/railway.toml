[build]
builder = "DOCKERFILE"
dockerfilePath = "services/strapi/Dockerfile"

[deploy]
healthcheckPath = "/_health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
